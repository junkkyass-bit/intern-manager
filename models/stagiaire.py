class Stagiaire:
    def __init__(self, matricule, nom, prenom, adresse, tel, datenaissance, class_id):
        self.matricule = matricule
        self.nom = nom
        self.prenom = prenom
        self.adresse = adresse
        self.tel = tel
        self.datenaissance = datenaissance
        self.class_id = class_id
